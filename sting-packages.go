package main
import (
	"fmt"
	"strings"
)

func main() {
	// Unicode Packages
	/*
		strings.IsDigit()
		strings.IsSpace()
		strings.IsLetter()
		strings.IsLower()
		strings.IsPunct()

		strings.ToUpper()
		strings.ToLower()

		strings.HasPrefix()
		strings.HasSuffix()

		strings.Index()
		strings.Replace()
		strings.TrimSpace()
	*/

	// Strconv Package
	/*
		Atoi()  // converts string to int
		itoa()  // converts int (base 10) to string
		FormatFloat(f, fmt, prec, bitSize)  // converts floating point number to a string
		ParseFloat (s, bitSize)  // Converts a string to a floating point number
	*/
}